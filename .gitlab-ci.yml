before_script:
  - python3 --version
  - python3 -m pip install -r requirements.txt

stages:
  - Static Analysis

flake8:
  stage: Static Analysis
  image: "python:3.7"
  tags: ["dev"]
  script:
  - flake8 --max-line-length=120 *.py
