stages:
  - Static Analysis

flake8:
  stage: Static Analysis
  image: "python:3.7"
  tags: ["dev"]
  script:
  - python3 --version
  - pip install -r requirements.txt
  - flake8 --max-line-length=120 *.py
